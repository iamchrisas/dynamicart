<h1>{{appTitle}} 🚀</h1>
<p>Welcome Ironhacker!</p>


<h1>All Posts</h1>

{{#each posts}}
  <div>
    <h2>{{this.title}}</h2>
    <a href="/user/{{this.user._id}}">{{this.user.username}}</a>
    <img src="{{this.image}}" alt="{{this.title}}">
  </div>
{{/each}}